import test from 'node:test';import assert from 'node:assert/strict';import{buildTeamPublishCandidate,formatTeamPublishCandidateReview}from'../src/application/team_publish_candidate.js';
const manifest={blueprint:{title:'Conference Planning Team',topology:{pattern:'builder_reviewer_synthesizer',execution_pattern:'sequential_review'},artifact_contract:{explicit_request_required:true},memory_plan:{surfaces:[{surface_id:'shared_core',title:'Core memory',semantic_slots:['plan','decisions']},{surface_id:'icde_public_context',title:'ICDE public conference info',semantic_slots:['conference','public'],visibility:'public'},{surface_id:'user_itinerary',title:'User travel itinerary',semantic_slots:['user','travel']}]}},team:{team_name:'Conference Planning Team',agents:[{agent_id:'planner',name:'Planner',role:'builder',purpose:'Builds conference planning checklists.'},{agent_id:'reviewer',name:'Reviewer',role:'reviewer',purpose:'Checks freshness and risk.'}]}};
test('team publish candidate separates public knowledge, private memory, and schema-only surfaces',()=>{const out=buildTeamPublishCandidate(manifest);assert.equal(out.review.summary.agents,2);assert.equal(out.review.summary.optional_knowledge_packs,1);assert.equal(out.review.summary.private_exclusions,1);assert.equal(out.review.summary.schema_only_surfaces,1);assert.equal(out.candidate.clone_policy.private_memory,'fresh_on_clone');assert.match(out.review.publish_as_knowledge_pack[0].surface_id,/icde/);assert.match(out.review.keep_private[0].surface_id,/user_itinerary/)});
test('team publish candidate review is glanceable text',()=>{const text=formatTeamPublishCandidateReview(buildTeamPublishCandidate(manifest));assert.match(text,/Team publish preview/);assert.match(text,/optional knowledge packs: 1/);assert.match(text,/Keep private:/);assert.match(text,/ICDE public conference info/)})
